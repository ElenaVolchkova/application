from django.conf import settings
from django.db import models
# from mysite.athletes.models import athletes
# from mysite.amazons.models import amazons
# from mysite.customers.models import customers


class Titova(models.Model):
    pass
    # athletes = models.ForeignKey(athletes, blank=True, on_delete=models.CASCADE)
    # amazons = models.ForeignKey(amazons, blank=True, on_delete=models.CASCADE)
    # customers = models.ForeignKey(customers, blank=True, on_delete=models.CASCADE)
    #
    #
    # def __str__(self):
    #     return self.ward
